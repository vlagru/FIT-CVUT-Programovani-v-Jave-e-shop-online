/*
package alsa.deprecated;

import alsa.entity.Notebook;
import alsa.entity.NotebookCategory;

public class ImmutableObjectExampleAddChanges {

    public static void printNotebook(Notebook notebook) {
        Notebook notebookCopy = new Notebook(notebook.getName() + " +",notebook.getPrice(),notebook.getCategory(), category);

        System.out.printf("%s %d %s\n", notebookCopy.getName(), notebookCopy.getPrice(), notebookCopy.getCategory());
    }


   public static void main (String[] args) {
       Notebook lenovo = new Notebook("Lenovo 12",30_000,NotebookCategory.PROFESSIONAL, category);

       printNotebook(lenovo);
       printNotebook(lenovo);
    }
}
*/